# === macOS ===
.DS_Store
.thumbnail
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride
Icon?

# === Xcode / сборка ===
build/
DerivedData/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
*.xcscmblueprint
*.xccheckout

xcuserdata/
*.xcuserstate

# Проект генерируется из project.yml через ./scripts/bootstrap.sh — в гите не нужен.
# Версии пакетов при этом закреплены в корневом Package.resolved.
*.xcodeproj/

# Team ID и прочие локальные настройки сборки (см. Config/Local.xcconfig.example).
Config/Local.xcconfig

# Собранные установщики — раздаём через релизы, а не через репозиторий.
installer/

# === Swift Package Manager ===
.build/
.swiftpm/
Packages/
Package.pins

# === CocoaPods / Carthage (на будущее) ===
Pods/
Carthage/Build/

# === Claude Code (локальные настройки этой машины) ===
.claude/settings.local.json

# === Прочее ===
*.log
*.tmp

# Дизайн-макеты и инструкции для ассистента — не в гит
design/
CLAUDE.md
