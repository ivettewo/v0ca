# === macOS ===
.DS_Store
.thumbnail
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride
Icon?

# === Xcode / build ===
build/
DerivedData/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
*.xcscmblueprint
*.xccheckout

xcuserdata/
*.xcuserstate

# The project is generated from project.yml via ./scripts/bootstrap.sh — no need to track it.
# Package versions are pinned in the root Package.resolved.
*.xcodeproj/

# Team ID and other machine-local build settings (see Config/Local.xcconfig.example).
Config/Local.xcconfig

# Built installers — distributed via releases, not the repository.
installer/

# === Swift Package Manager ===
.build/
.swiftpm/
Packages/
Package.pins

# === CocoaPods / Carthage (just in case) ===
Pods/
Carthage/Build/

# === Claude Code (machine-local settings) ===
.claude/settings.local.json

# === Misc ===
*.log
*.tmp

# Design mockups and assistant instructions — not tracked
design/
CLAUDE.md
